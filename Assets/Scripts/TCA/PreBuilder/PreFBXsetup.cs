using UnityEngine;

public class PreFBXsetup : MonoBehaviour
{
    // Base é o próprio objeto
    // Instanciar as tábuas da ponte X distancia da outra a partir da base
    // Tenho que passar o tamanho int de tábuas e o prefab da tábua
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
